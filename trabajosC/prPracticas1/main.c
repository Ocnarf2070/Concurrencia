/*
 * main.c
 *
 *  Created on: 10 mar. 2017
 *      Author: franc
 */

#include <stdio.h>
#include "gestion_memoria.h"
/*
int main(){
	setvbuf(stdout, NULL, _IONBF,0);
	setvbuf(stderr, NULL, _IONBF,0);
	unsigned ok,dir;
	T_Manejador m;
	crear(&m);
	mostrar(m);
	obtener(&m,50,&dir,&ok);
	mostrar(m);
	obtener(&m,80,&dir,&ok);
	mostrar(m);
	obtener(&m,1000,&dir,&ok);
	mostrar(m);
	destruir(&m);
	mostrar(m);
	return 0;
}*/
